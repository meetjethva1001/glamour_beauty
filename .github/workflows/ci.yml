# name: Glamour Studio CI/CD

# on:
#   push:
#     branches: [main, develop]
#   pull_request:
#     branches: [main]

# jobs:
#   frontend:
#     name: Frontend Build & Lint
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: frontend

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: frontend/package-lock.json

#       - name: Install dependencies
#         run: npm ci

#       - name: Lint
#         run: npm run lint

#       - name: Build
#         run: npm run build

#       - name: Build Docker image
#         run: docker build -t glamour-frontend .

#   backend:
#     name: Backend Lint & Docker
#     runs-on: ubuntu-latest
#     defaults:
#       run:
#         working-directory: backend

#     steps:
#       - uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm'
#           cache-dependency-path: backend/package-lock.json

#       - name: Install dependencies
#         run: npm ci

#       - name: Build Docker image
#         run: docker build -t glamour-backend .
